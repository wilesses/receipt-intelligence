# Current Context

## Confirmed state

- Git-state reconciliation was completed locally on 2026-08-30: every intended public path now has one staged final version, with zero mixed, unstaged, or untracked paths. The obsolete `PROJECT_GUIDE - Copy.md` was removed from the index only and remains local; no commit, push, publication, history rewrite, or production database write occurred.
- Public-release packaging was completed locally on 2026-08-28 without commit, push, publication, local-file deletion, or history rewrite. Approved private/generated categories were removed from the Git index only; every excluded local path was verified present afterward. `.gitignore` now covers private receipt/OCR data, databases and sidecars, audits, temporary outputs, personal workspaces, and local agent state without broadly ignoring images.
- `RECEIPT_DB_PATH` can select a disposable runtime database before imports; the default remains `data/receipts.db`. `python -m app.create_sample_db --output <path>` builds a guarded synthetic database from `sample_data/receipts.json`; the current sample has 3 receipts, 7 items, 2 synthetic stores, and one recurring normalized product with two comparable price observations.
- Public portfolio artifacts now include `README.md`, six read-only queries in `analysis/queries.sql`, and three UI screenshots under `docs/images/`, all rendered from the synthetic database. Screenshot OCR confirmed only synthetic store, receipt, and product identifiers.
- Verification passed: 297 unittests, focused sample tests, six SQL queries in SQLite read-only mode, Python compilation, `git diff --check`, sample integrity, and Flask startup HTTP 200 on an isolated port. Production DB remained byte-identical at SHA256 `FBE3E42923700A53BC91AA00194D87A0FA06EDF020238E89DE7D4B48BFD2B343`; read-only integrity is `ok`.
- `HEAD`, `main`, and `codex/category-classifier-v2` point to `fc94a6c`; reachable branch history has no database, PDF, or raster-image paths. Private-looking unreachable Git objects remain in the local object store and were not rewritten by request. Publish only the reviewed branch; do not use broad mirror workflows. Security review also found local-only deployment warnings (CSRF on write routes, unbounded PDF ingestion, and a Referer-based redirect) that remain outside this packaging change.
- Main database: `data/receipts.db`, 379 receipts, 3757 items, 745 category rules, and 1425 effective identities. After the approved six-row derived Price Model repair on 2026-08-25, SHA256 is `FBE3E42923700A53BC91AA00194D87A0FA06EDF020238E89DE7D4B48BFD2B343` and integrity is `ok`. Later user data means older audit projections below remain point-in-time unless explicitly refreshed.
- Derived-only arithmetic mismatch repair is applied for exact item IDs 3577, 3615, 3670, 3671, 3674, and 3675. Only effective `unit_price` changed, plus deterministic parser confidence 0.70→0.95 for #3577; source remains `parser`. Quantity, `price`, `line_total`, normalization, package, identity, category, receipt, and raw evidence fields are unchanged. Arithmetic mismatches fell from 10 to 4; remaining rows are 3575 REVIEW, 3613 REVIEW, 3614 REVIEW, and 3668 PROTECTED.
- Verified Backup API copy: `data/backups/receipts_before_price_arithmetic_safe_repair_20260825_174326_044172.db`, SHA256 `6916F7292E061526F8B6D6C0A8EE3F89C7954F87CB318D7B549F7453C486EDEE`, integrity `ok`. Strict comparison found exactly six allowlisted item changes and zero out-of-allowlist, receipt, rule, raw, or financial changes. All six become normalized-price eligible, with one July trend point each; all remain 0/3 insufficient history and `only_one_store`. Store Comparison readiness remains 3 COMPARABLE / 2 PRELIMINARY. See `docs/audits/2026-08-25-price-arithmetic-mismatch-audit.md`.
- One executable effective-product-identity contract lives in `app/product_identity.py`: nonblank persisted `canonical_name`, otherwise raw receipt `name`. `normalized_name` remains search/matcher support and is never an automatic user-facing merge. Category `product_key` remains a separate normalized rule-grouping contract.
- Product Dossier, Analytics, autocomplete, Home product grouping, deviation evaluation, Store Price Comparison, product discovery, and Merge now consume the shared effective identity expression or row helper. Exact identity wins; a raw alias resolves only when it maps unambiguously to one effective identity.
- Immutable production projection: 1412 effective identities = 36 canonical-represented + 1376 raw-only; 1433 raw aliases, 1423 unambiguous and 10 ambiguous; 0 SQL/helper row mismatches and 0 exact-resolution failures. `normalized_name` has 1424 distinct values but does not define identity.
- Normalized Analytics trend is ready for 1033 identities (810 kg, 187 L, 36 piece), covering 2348 eligible observations and 1726 monthly median points; 376 identities have no comparable history and 3 fail closed on unresolved identity. Under an exact-group replay, 379 identities that the removed arithmetic formula could plot are now intentionally unavailable instead of falling back. Store Comparison remains 5 eligible groups: 3 COMPARABLE and 2 PRELIMINARY.
- Browser verification against a disposable production snapshot passed the full 1440×1000/390×844 × light/dark matrix. Cases: canonical/store-comparable `Burkani sverami kg` €/kg; canonical `Pepsi 1L` €/L; raw-only insufficient multipack `Rīsi VALDO kārba 8x125g`; raw-only outside-top-10 `Kartupeļi frī AVIKO Zig Zag 750g`; and raw alias → canonical `NONGSHIM Kimchi`. Analytics and Dossier showed exact identities, honest insufficient states, explicit units, paid-total summary, no competing legacy average/ambiguous unit label, zero horizontal overflow, console/page errors, or failed local application requests. The in-app browser runtime itself was unavailable because its Windows sandbox helper failed before connection; bundled Playwright completed the same local UI verification.
- Category Taxonomy v2 is applied to production from the exhaustive exact SAFE manifest: 1,344 identities / 3,563 item rows were changed; 130 exact rules were updated and 603 inserted. REVIEW 39/65 and UNRESOLVED 2/2 remain persisted unchanged. The application exposes exactly 18 flat top-level categories; excluded old values report as `прочее / требует решения` while editors retain their stored-label context. The historical migration itself changed no classifier, schema, receipt, identity, price, Price Model, or `category_source` field.
- Category Classifier v2 is implemented for future imports only. It uses Unicode token/phrase/prefix matching, one contribution per keyword, product-type-over-flavour precedence, narrow guards for eggs/fish/pantry/tomato paste/instant noodles/frozen fries/pet/baby/non-food/alcohol, and fail-closed unresolved fallback. Parser categorization delegates to the shared classifier. `product_category_rules` remain authoritative and persist as `inherited`; no historical category was reclassified.
- Immutable production shadow: overall 3,251/3,701 rows agree (87.84%), 8.51% fallback; exact-rule cohort 2,230/2,585 (86.27%); rule-free cohort 1,021/1,116 (91.49%), 10.04% fallback and 630/698 exact identities. Exact rules protect 370 classifier disagreements. Golden/collision set covers 80 names with zero failures and zero obvious reviewed false positives. See `docs/audits/2026-08-18-category-classifier-v2-shadow-evaluation.md`.
- Category Classifier v2 verification: 132 affected tests and full 260-test suite pass. Production DB stayed byte-identical at SHA256 `EFDE4EED5E357EF60135C057C73DB724C6FD4C7A1FCCB15AF1A7F88BB05C5BBB`; integrity `ok`; 374 receipts, 3,701 items, 745 rules, 1,403 effective identities, €7,985.24 spend, and no WAL/SHM/journal sidecars.
- Category migration pre/post SHA256: `447A8C927E566B2D08C491EFAD286CBD633F04CA55CFF7FF932A3E5BE8E96DD9` → `EFDE4EED5E357EF60135C057C73DB724C6FD4C7A1FCCB15AF1A7F88BB05C5BBB`. Verified backup: `data/backups/receipts_before_category_taxonomy_v2_20260818_182832_901493.db`, SHA256 `71E7A92B26EEE23BC2C956D0932317FCCBBB38A5C66F0F919F150A2C67C241A8`, integrity `ok`. Counts/€7,985.24 spend are unchanged and strict backup comparison found zero out-of-scope writes. See `docs/audits/2026-08-18-category-taxonomy-v2-apply-result.md`.
- Category Taxonomy v2 verification: 123 focused tests and the full 252-test suite pass; Python compilation, affected JavaScript syntax checks, category audit, and `git diff --check` pass. Browser checks covered Home, Analytics, Receipt Detail, Product Dossier, and Review in all four 1440×1000/390×844 × light/dark combinations with 18 selector and chart labels, exact egg filtering (€69.06), a working Home→Analytics category link, legacy-value context, zero console/page errors, zero failed requests, and zero horizontal overflow. Because the sandbox blocks CDN access, the same CDN asset versions were supplied from a temporary directory for the clean browser run; project files and dependencies were not changed.
- Parser Hardening v2 is implemented for new imports. Persisted `price == line_total`; effective `unit_price` is paid total divided by quantity; normalized price uses paid total. Fixed €0.02 mismatch validation caps normalization confidence below 0.75.
- Measurement parsing recognizes age/package tokens only for ages 1–36 (`6+110g` -> 110 g), keeps `x`/`х`/`×` multipacks unresolved, and treats supported fractional terminal `45+ kg` forms as weighted evidence without producing a 45000 g package.
- Receipt Detail, Product Dossier, Purchase Register, and Price Quality use explicit paid-total/effective-unit/normalization terminology. Price Quality detects persisted arithmetic mismatch independently of stored confidence and supports `COALESCE(line_total, price)` for legacy rows.
- Shared historical price deviation is implemented in `app/price_deviation.py` and used by Receipt Detail, Product Dossier latest-vs-usual, and Home Month Story normalized-price candidates. Contract: persisted canonical-or-raw effective identity; normalized EUR/kg, EUR/L, or EUR/piece only; sources `derived`, `package_name`, `parser`, `weighted_inference`, or guarded `manual_correction`; confidence `>= 0.75`; positive consistent quantity/unit price/line total within €0.02; valid date/store; no blocking Price Model diagnostic or known unresolved identity; same unit; previous 180 days; minimum 3 eligible prior observations; median baseline; same-day order `(date, receipt_id, item_id)`; thresholds `<= -10%` cheaper and `>= +15%` more expensive. Current observation never enters its own baseline and no legacy `price / quantity` fallback can create a claim.
- Shared `INSUFFICIENT_HISTORY` presentation lives in `app/price_deviation_presentation.py` and is used by Receipt Detail and Product Dossier. Categories are `NOT_ENOUGH_HISTORY`, `PRICE_NOT_COMPARABLE`, `PRODUCT_IDENTITY_UNCERTAIN`, and `EVIDENCE_NEEDS_REVIEW`. Precedence remains evaluator-owned and deterministic: unresolved identity, then current-observation eligibility, then insufficient prior history. Only `NOT_ENOUGH_HISTORY` shows progress; `eligible_prior_count` means eligible normalized observations for the same effective identity and unit, strictly prior to the current row within 180 days. Home continues to suppress insufficient price candidates.
- Read-only production presentation projection over all 3686 observations: 1779 `NOT_ENOUGH_HISTORY`, 1340 `PRICE_NOT_COMPARABLE`, 29 `PRODUCT_IDENTITY_UNCERTAIN`, and 17 `EVIDENCE_NEEDS_REVIEW`. History progress is 1134 at 0/3, 438 at 1/3, and 207 at 2/3. Samples from each category were checked against their machine reasons, including missing normalized price, unresolved identity, ambiguous measurement, low confidence, parser contamination, and arithmetic mismatch.
- Home keeps its existing story ranking, relevance checks, and additional `abs(deviation) >= 15%` presentation gate. Store Price Comparison remains a separate per-store median evaluator. Analytics spend remains separate; the item trend now uses exact effective identity plus the shared safe normalized-price eligibility and monthly medians, with no `price / quantity` fallback.
- Read-only production projection: 1402 effective groups and 3686 observations; 2300 current observations pass normalized evidence/identity eligibility; 521 have sufficient prior history, producing 354 NORMAL, 98 CHEAPER, and 69 MORE_EXPENSIVE results; 3165 are INSUFFICIENT. Receipt-item old/new comparison: 1831 unchanged, 239 changed through self-baseline removal, 966 through removal of legacy denominator fallback, 1 through current safe-quality rejection, and 649 other changes driven mainly by the common 180-day/minimum-history contract.
- Shared price-deviation verification now covers 148 focused tests. Evidence-presentation browser verification covers 24 production-snapshot states: Receipt 0/3, 1/3, price-not-comparable, identity-uncertain and review; Dossier 2/3, current-price-ineligible and successful deviation at 1440px and 390px in light/dark themes. All cases returned 200 with no app console/page errors, text or horizontal overflow. Full suite: 243 tests, zero failures. Production DB stayed read-only.
- Receipt #374 is covered by a sanitized 11-product golden fixture: product rows total €30.81; excluded paper-bag service line €0.19 reconciles to header total €31.00; all three RŪDOLFS rows resolve to 110 g.
- Parser Hardening corpus projection was read-only over 3686 rows: 3 newly resolved unspaced age/package rows, 85 multipack candidates kept unresolved, 5 deterministic weighted projections, 10 existing arithmetic conflicts, and 0 reviewed new false-positive age/package resolutions. Production DB SHA256 remained `BBBC449848310319837C4B84176848E530F52217514E460E0D491A2B50991F35`.
- Historical Price Model Backfill analysis is complete and read-only over all 3686 items: 8 future SAFE allowlist candidates, 7 REVIEW, 1338 UNRESOLVED, and 2333 PROTECTED. The three unspaced `6+110g` rows remain REVIEW because a package-only fill would retain an inconsistent €/piece denominator. No backfill was executed; report and row-level CSVs are under `docs/audits/2026-08-08-historical-price-model-backfill-*`; production DB SHA256 remained `BBBC449848310319837C4B84176848E530F52217514E460E0D491A2B50991F35` with integrity `ok`.
- Production Receipts archive (`/?view=receipts`) now uses the Receipt Workspace composition: compact header, supported GET filters, document-like receipt rows, inline expansion with real item previews, honest evidence summary, and drawer for currently available receipt/extraction state.
- The Receipts archive includes a compact current-month evidence band above filters: real monthly spend, secondary forecast eligibility state, receipt/item-line counts, and Top 3 products by month spend.
- The archive keeps existing URLs and behavior: `/` with `view=receipts`, `period`, `store_search`, upload link, return to overview, client sorting, and `/receipt/<id>` detail fallback.
- `items.normalized_name` is filled for all 3580 items.
- Selective Price Model backfill is complete. Only `package_name` and `weighted_inference` were applied: 2184 rows updated (`package_name`: 2179, `weighted_inference`: 5).
- Resulting selected-source distribution: `eur_per_kg` 1664, `eur_per_l` 452, `eur_per_piece` 68.
- `inferred_piece`, `service_line`, rejected/suspicious and unresolved results were not applied.
- Manual-review item IDs 3544, 3578, 3579 and 3581 were preserved.
- Repeated selective dry-run for `package_name,weighted_inference` reports 0 rows to update.
- 1094 rows still have unresolved normalized price; excluded audit groups include service lines, inferred pieces, rejected/ambiguous data and four high-confidence conflicts.
- Parser Quality v2 is implemented at the shared `parse_receipt()` boundary. Safe decimal whitespace is repaired; receipt/cashier/service lines and proven malformed OCR candidates are rejected before persistence.
- `lkg`, `m1`, unsafe split decimals and glued package tokens are never guessed. Valid multipacks remain unchanged and unresolved.
- Read-only `python -m app.audit_parser_quality` projects 8 corrected, 694 rejected, 138 unresolved and 2740 unchanged rows from the current 3580 stored names.
- Analytics Chart.js lifecycle now reconciles both the local instance map and the Chart.js canvas registry before every remount. Repeated filters, recovery from an empty slice, and dark/light theme rerenders preserve the current datasets without leaking canvas ownership.
- Analytics lifecycle verification passes: 8 focused unit/UI tests and a browser sequence covering all purchases, `фрукты`, reset, Rimi, an existing product, an empty product result, recovery, and theme rerender.
- Analytics Local Research is a universal Product Dossier discovery entry. `/autocomplete/item_names` searches effective identity, raw aliases, and `normalized_name`, but returns only exact shared effective identities; `details=1` adds the Flask-generated Dossier URL. Current production coverage is 1412 authoritative identities and all are exactly addressable. A valid selection enables the normalized trend and Dossier actions; editing input immediately invalidates both.
- Product Dossier now uses `/item/<path:name>` and relies on Flask's single path decoding. Spaces, Latvian characters, `%`, `+`, punctuation, and persisted names containing `/` resolve without changing identity or Dossier semantics. Existing links remain valid.
- Local Research browser verification covers a top-10 canonical product, a product outside top 10, cross-store canonical `Burkani sverami kg`, raw-name fallbacks, Latvian percentage punctuation, and two slash-sensitive identities at 1440px/390px in light/dark themes. All 24 search/trend/Dossier/Back/stale-selection cases plus the top-10 shortcut passed with no horizontal overflow or app console/page errors.
- Motion cleanup is complete for the application shell, Archive drawer/disclosure, Upload progress, and Analytics product transitions. Global smooth scrolling and blanket `.01ms` reduced-motion overrides are removed; drawer exit completes before `hidden` and restores focus; Analytics chart/container height transitions are absent.
- The application color system now uses one Home-derived canonical `--color-*` palette: charcoal/graphite backgrounds and surfaces, neutral borders, teal primary/active/focus states, and separate success/warning/danger semantics. Legacy component aliases, Analytics chart fallbacks, shared navigation, Upload, and Category Review consume the same tokens in dark and light themes; browser verification covered `/`, `/analytics`, `/upload`, and `/products/review` at 1440×1000 dark/light and 390×844 dark with no overflow or page errors.
- Active application templates now use one local Lucide icon sprite through `app/web/templates/_icons.html`; the Bootstrap Icons stylesheet is no longer loaded. Icons inherit semantic color, remain decorative beside existing text labels, and require no runtime package or CDN. Exact pre-migration files are preserved under `tmp/backups/icon-migration-20260825-before-lucide/` for rollback. Browser verification covered Home, Archive, Analytics, Upload, and Category Review across desktop/mobile and dark/light: the local sprite returned 200, visible icons stayed at least 16px, and there were no application console errors or horizontal overflow.
- Category Review (`/products/review`) now uses the Review Workspace composition: compact header, preserved GET toolbar, one queue summary surface, document-like decision items, real review reasons, existing category confirmation POST, links to existing item/receipt routes, and a completed empty state. No category logic or backend behavior changed.
- Shared Jinja review primitives live in `app/web/templates/_review_components.html`: status, reason, summary, actions, and review item. They are designed for later Suggestions/Merge/Price Quality migration without forcing one universal layout.
- Category Review browser verification covers populated/empty queues, long names, 375/390/768/desktop widths, dark/light themes, 44px controls, duplicate IDs, horizontal overflow, and console errors. The in-app browser could not reliably synthesize Tab/Enter for the hidden skip-link; native control semantics and structural tests remain verified.
- Suggestions (`/products/suggestions`) uses a Comparison Workspace composition: compact preserved GET filters, one queue summary, symmetric product pairs, primary difference signals, expandable matcher reasons, existing item links, the existing merge destination, and transient client-only dismiss. Route, merge behavior, UI composition, and database state remain unchanged.
- Product matcher hardening keeps the existing manual Suggestions/Merge architecture. Candidate buckets now use a matcher-only accent-folded raw-name form; persisted `normalized_name` is unchanged. Text score is the equal blend of `token_set_ratio` and `token_sort_ratio`; `high` additionally requires symmetric significant-token coverage, and reasons expose full-name symmetry/unmatched tokens.
- Symmetric corpus-backed variant guards now cover audited one-sided Zero/Max, Latvian flavor markers, young produce, quality grade, egg L/M size, and caliber evidence. Raw-name multipack signatures recognize `x`/`х`/`×`; single-pack/multipack, different multipacks, explicit `ml`/`g`, size, percentage, and variant conflicts are blocked before scoring. Malformed OCR units remain unguessed.
- Read-only production evaluation changed suggestions from 420 (`173 high`, `247 possible`, 50 cross-store) to 264 (`87 high`, `177 possible`, 81 cross-store). The matcher recovered 19/26 audited SAME_PRODUCT false splits; 9/10 representative dangerous pairs disappeared and the remaining generic/branded egg pair moved from `high` to `possible`. All five tracked good pairs remain suggestions.
- The audited cross-store canonical cleanup is applied. Exactly 176 allowlisted `items.canonical_name` values were split into 23 reviewed target batches in one transaction; raw `name`, `normalized_name`, categories, price fields, receipts, schema, matcher, parser, UI, and analytics formulas were unchanged. The two SKU-safe groups and ambiguous `Dilles 100g` canonical group remain untouched; the malformed Coca-Cola ZIP multipack is isolated under its reviewed verbatim label without SKU inference.
- Manual category intent is preserved for every moved subgroup that had an old manual product rule. Nineteen exact `product_category_rules` rows were inserted, the existing `burkani sverami kg` rule was verified, and all 122 prior rules were retained unchanged. Post-cleanup readiness across the same 32 groups is 3 READY, 26 NOT_READY_PRICE_DATA, 1 NOT_READY_IDENTITY, and 2 AMBIGUOUS.
- Pre-cleanup production SHA256 was `BBBC449848310319837C4B84176848E530F52217514E460E0D491A2B50991F35`; post-cleanup SHA256 is `05FF22F03019857FE2E321C76ABE704F978495ED4882D5F6FB7AE9FE1AF9899D`. Verified SQLite Backup API backup: `data/backups/receipts_before_canonical_cleanup_20260809_184059_032672.db`, SHA256 `428EB7482A6A91B120FCAFF3A860361CC9B17CAFD737BFD68575C1C983BAA2D6`, integrity `ok`.
- Suggestions reuses the Review status, reason, and summary primitives while keeping a dedicated `ComparisonPair` macro in `app/web/templates/_comparison_components.html`; it intentionally does not reuse the decision-form layout from Category Review.
- Suggestions browser verification covers real populated data, filtered no-results, 50 rendered pairs, long names, desktop/mobile pair structure, dark/light themes, duplicate IDs, horizontal overflow, native disclosure markup, and console errors. The in-app browser still could not reliably synthesize keyboard Tab focus; native links/buttons/details and visible focus styles remain structurally verified.
- Product Merge (`/products/merge`) now uses a Review / Bulk Selection Workspace: compact header and search toolbar, dense selection records, selected state expressed by text and structure as well as color, a desktop-sticky/mobile-flow command band, selected-name list, row and receipt-link impact summaries, visible canonical-name label, whitespace validation, reset, and the unchanged merge POST action.
- Merge-specific Jinja primitives live in `app/web/templates/_selection_components.html`; the selection lifecycle is isolated in `app/web/static/product-merge.js`. The route, query shape, merge update semantics, database, parser, and normalized product logic were not changed.
- Merge browser verification covers two selected products, selected totals, whitespace-only canonical-name validation, reset with focus restore, filtered no-results, long names, 1440/390/360 widths, dark/light themes, no horizontal overflow, and no page exceptions. Browser CDN requests are denied by the test sandbox, so screenshots simulate the normal collapsed Bootstrap mobile navigation while leaving application files unchanged.
- Price Quality (`/data-quality/prices`) keeps its five GET filters and diagnostic register, and now classifies every shown row as `CORRECTABLE_V1`, `NOT_CORRECTABLE_V1`, or `ALREADY_SAFE`. The first narrow workflow edits only structured quantity/package evidence for one row; it never edits raw receipt identity, quantity, paid price, line total, category, or receipt fields.
- Correctable v1 excludes missing `line_total`, arithmetic mismatch, unresolved multipack, service/parser contamination, and every proposal that still produces a blocking Price Model warning. Current read-only production coverage is 12 `CORRECTABLE_V1`, 1345 `NOT_CORRECTABLE_V1`, and 0 `ALREADY_SAFE` among the 1357 broad problematic rows.
- Preview is write-free. Its signed token is an integrity/stale-state mechanism, not a security boundary: it contains item ID, a hash of persisted before-state, proposed structured fields, and projected derived fields. Apply reloads the row inside `BEGIN IMMEDIATE`, verifies the hash and proposal, reruns `derive_price_data()`, compares the fresh projection, and updates exactly one row through optimistic before-state predicates.
- Applied rows use `price_parse_source = manual_correction` and `price_parse_confidence = 0.85`. This is policy confidence for user-confirmed structured evidence that passed the same arithmetic, multipack, contamination, and normalization guards; it is not a claim of 85% factual certainty. Eligible corrected observations may then enter the existing Analytics, Dossier, deviation, and Store Comparison contracts.
- The current arithmetic mismatch set was audited read-only against all three source PDFs: 10 rows, all caused by Rimi discount semantics (gross listed price persisted as `unit_price`, paid `Gala cena` persisted as `price`/`line_total`). Six derived-only SAFE candidates are IDs 3577, 3615, 3670, 3671, 3674, and 3675; 3575/3613/3614 require review and 3668 stays protected as unresolved multipack. Correction v1 cannot apply the SAFE refresh because it blocks existing arithmetic mismatch before preview. See `docs/audits/2026-08-25-price-arithmetic-mismatch-audit.md`.
- Item Profile (`/item/<name>`) now uses a document-like Product Dossier composition: compact identity, one summary surface, a dominant Price Story, a purchase register, and related stores/names/format context. Existing category forms, receipt links, route, SQL, Price Model, normalized-product logic, and database behavior remain unchanged.
- Product Dossier primitives `ProductSummary`, `PriceStory`, and `PurchaseRegister` live in `app/web/templates/_product_dossier_components.html`; Chart.js lifecycle and honest empty/error states live in `app/web/static/item-profile.js`. The summary exposes paid total, not a misleading legacy average. The monthly chart is the same safe normalized-price median series as Analytics and shows its denominator explicitly.
- Product Dossier includes read-only Store Price Comparison. `app/store_price_comparison.py` groups only by the shared persisted effective identity from `app/product_identity.py`, rejects known unresolved identities and mixed canonical provenance, and uses only persisted positive normalized prices with a known common denominator, supported source, confidence `>= 0.75`, consistent `quantity * unit_price == line_total` within €0.02, and no blocking Price Model diagnostics. It never falls back to legacy `price / quantity`.
- Evidence levels are deterministic across every participating store: PRELIMINARY requires at least 1 eligible observation/store and withholds winner/difference; LIMITED requires at least 2/store and qualifies the observed median difference; COMPARABLE requires at least 3/store and may state the historical median winner. Equal medians never produce a winner; more than two stores rank by median then store name and compare a unique cheapest store with the runner-up.
- Last dedicated Store Price Comparison audit covered 1402 effective groups, 14 with two stores, and 5 eligible comparisons: 3 COMPARABLE and 2 PRELIMINARY, with 0 LIMITED. Newly eligible `Burkani sverami kg` was COMPARABLE: MAXIMA 7 observations, median 0.3989 €/kg, range 0.2361–0.9697; RIMI 8, median 0.8865 €/kg, range 0.6410–0.9927; MAXIMA's historical median was 55.0% lower. Newly eligible `Ingvers kg` was PRELIMINARY: MAXIMA 1 at 5.9483 €/kg; RIMI 3, median 5.9524 €/kg, range 5.9259–6.0294; no winner claim was allowed. Ineligible reasons were 1387 `only_one_store`, 7 `insufficient_comparable_prices`, and 3 `unresolved_product_identity`.
- The audited cross-store Price Coverage repair is applied to exactly 19 highest-value SAFE_REPAIR rows: all 15 `Burkani sverami kg` and all 4 `Ingvers kg`. One transaction changed only eight previously NULL Price Model fields (`line_total`, `unit_price`, `quantity_unit`, `package_unit`, `normalized_unit_price`, `normalized_price_unit`, `price_parse_source`, `price_parse_confidence`); identity, quantity, paid `price`, categories, receipts, rules, schema, parser, and comparison logic were unchanged. Comparison with the verified backup found zero out-of-allowlist writes. The three SAFE `Gurki īsie` rows were intentionally deferred and remain blocked; all six beverage OCR groups remain unresolved/fail-closed, with no interpretation of `1,51` or `21` as litres. See `docs/audits/2026-08-09-cross-store-price-coverage-*`.
- Price Coverage repair pre-write SHA256 was `05FF22F03019857FE2E321C76ABE704F978495ED4882D5F6FB7AE9FE1AF9899D`; post-write SHA256 is `EE3CEFBAB91DDA21C4A28BC9E50CA41921726D0B63423F63DCD0F6363AD0D3CD`. Verified SQLite Backup API backup: `data/backups/receipts_before_cross_store_price_coverage_repair_20260809_201126_115450.db`, SHA256 `0F38FE8D6F3EB9D27C3EF3941A6DDC9438B0651DBA7CB41557E7C7B451F907CC`, integrity `ok`. Production integrity is `ok`; counts remain 372 receipts, 3686 items, and 141 category rules.
- Store comparison browser verification uses a database snapshot and covers COMPARABLE, PRELIMINARY, and unresolved ineligible states at 1000px and 390px in both light and dark themes. All six cases have zero horizontal overflow and zero console/page errors; Price Story, Purchase Register receipt links, and category controls remain functional. Production database contents and schema were not written.
- Item Profile browser verification covers a four-purchase/two-store product, a 47-purchase product, one purchase, a missing product, 1000/390/360 widths, dark/light theme rerender, 44px controls, duplicate IDs, horizontal overflow, and console errors. The route now reaches names containing `/`; it still cannot distinguish a missing product from a product with no purchases without backend changes.
- Upload (`/upload`) now uses a Focused Task Workspace: one primary PDF selection surface, an explicit file queue, one import command bar, and Gmail import as a visually secondary source. Existing `/upload` and `/gmail/fetch` endpoints, the multipart `pdfs` payload, importer, parser, routes, and database behavior remain unchanged.
- Upload interaction is isolated in `app/web/static/upload-workspace.js`. It preserves drag-and-drop and multiple-file selection, renders filenames and server results through safe DOM APIs, keeps progress on `transform: scaleX`, and exposes distinct empty, busy, success, warning, and error states without adding a frontend dependency.
- Upload browser verification covers empty and two-file queue states, a long filename, 1000/390/360 widths, dark/light themes, 44px controls, duplicate IDs, horizontal overflow, and console errors. A real import and Gmail fetch were intentionally not executed against production data; their existing request contracts are covered structurally and by route tests.
- Calendar-sensitive Archive/Home tests now use an explicit July 14, 2026 reference date through the narrow `TODAY_PROVIDER` application seam. Production defaults to `date.today`, and the route passes one reference date through story-month resolution and dashboard calculations. January-to-December year-boundary behavior is covered explicitly.
- The full suite currently runs 294 tests with zero failures. Python compilation, changed JavaScript syntax, palette token contracts, shared price-deviation, product identity, normalized trend, Dossier semantics, category migration/classifier, product matcher/normalizer, Analytics discovery, Suggestions/Merge route/UI, and Parser Hardening coverage all pass, including the golden receipt #374 fixture.
- Project-local ECC workflows `eval-harness`, `e2e-testing`, and `verification-loop` are installed under `.agents/skills/`; no ECC hooks, MCP servers, global Codex config, or broader profiles were installed.
- Ruflo `3.32.9` is prepared as an optional project-local Codex MCP with hierarchical coordination, a four-agent limit, initialized local SQLite memory, HNSW search, a project memory graph, safe hooks, local learning, and bounded daemon autostart. Hook auto-execution, scheduled AI workers, cloud MCP, federation, and global config changes remain disabled; Codex stays in `workspace-write` with on-request approvals. A new Codex session is required to load the MCP; see `docs/Ruflo.md`.
- SQLite Backup API created `data/backups/receipts_before_price_backfill_20260714_190009_160214.db`; `PRAGMA integrity_check` returned `ok`.
- After the selective Price Model backfill, the user manually changed data in the main database. Its current SHA256 therefore differs from the previously recorded post-backfill hash; this is not an unintended write by parser audit or tests.
- Historical PDF corpus reparse audit covers only `data/pdf_receipts/**/*.pdf`: 279 files, 246 unique SHA256 values, 24 duplicate groups, 136 OCR and 143 text-layer files. The production path parsed 278 files into an isolated 278-receipt/2107-item temporary DB; `31-3128172.pdf` yielded no items. Exact-stem reconciliation corroborates the existing eight-row SAFE allowlist at the final import boundary, keeps the three `6+110g` denominator changes in REVIEW, and leaves multipacks unresolved. Production DB and source PDFs were unchanged; see `docs/audits/2026-08-08-historical-pdf-reparse-audit.md`.

## Next recommended stage

Review the four remaining arithmetic mismatches individually: 3575, 3613, and 3614 require REVIEW; 3668 remains PROTECTED multipack evidence. Do not broaden the completed automatic repair.

## Do not run without explicit user approval

- Any full Price Model write-backfill.
- Any write-backfill including `inferred_piece`, `service_line`, rejected/suspicious or unresolved rows.
- Any clearing or replacement of high-confidence parser data or manual-review IDs 3544, 3578, 3579 and 3581.
- Any schema change, receipt import, database recreation, or Obsidian Vault modification.
- Any parser cleanup of historical rows or parser backfill without a separate audited plan and explicit approval.
- Any production repair of the audited arithmetic mismatch rows without a separate controlled apply plan and explicit approval.

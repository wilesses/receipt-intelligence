# Project Summary

## Что делает проект

Receipt Tracker v2 - локальный трекер расходов по магазинным чекам. Он принимает PDF-чеки, извлекает из них текст, парсит товары, назначает категории, сохраняет данные в SQLite и показывает их через Flask web UI.

Главный объект проекта - не просто чек как сумма, а товарная строка чека. Поэтому большая часть аналитики строится по таблице `items`: категории, топ товаров, цена за единицу, динамика цены и сравнение текущей цены с историей.

Проект умеет получать чеки двумя путями:

- вручную через страницу `/upload`;
- из Gmail через IMAP endpoint `/gmail/fetch`.

Также есть пакетный импорт из папки `data/pdf_receipts` через `app/importer.py`.

## Как устроен проект

Запуск начинается в `run.py`. Он создает Flask-приложение через `app/web/app.py:create_app()`. При создании приложения вызывается `app/db.py:create_tables()`, поэтому таблицы и индексы проверяются на старте.

Ключевые файлы:

- `app/web/routes.py` - маршруты, SQL для страниц, часть расчетов;
- `app/db.py` - подключение к SQLite, схема, вставка чеков;
- `app/importer.py` - извлечение текста из PDF, OCR, подготовка данных, сохранение;
- `app/receipt_parser.py` - парсинг Rimi и Maxima;
- `app/product_normalizer.py` - нормализация названий товаров;
- `app/product_matcher.py` - RapidFuzz suggestions для похожих товаров;
- `app/price_model.py` - Price & Quantity Model v2;
- `app/category_keywords.py` - словарь категорий и порядок приоритетов;
- `app/category_rules.py` - ручные категории товарных групп;
- `app/analytics_service.py` - данные для графиков `/analytics`;
- `app/gmail_fetcher.py` - получение PDF из Gmail;
- `app/web/templates/` - страницы интерфейса.

`app/analytics.py` сейчас пустой. Реальная аналитика не там. `app/analy` тоже пустой файл и кодом не используется.

Данные лежат в:

- `data/receipts.db` - SQLite база;
- `data/uploads` - PDF, загруженные через web UI;
- `data/pdf_receipts` - PDF из Gmail или пакетного импорта.

## Поток данных

```text
PDF / Gmail
↓
Извлечение текста
↓
Парсер
↓
Категоризация
↓
SQLite
↓
Analytics
↓
Web UI
```

### Импорт PDF

Ручной импорт идет через route `/upload` в `app/web/routes.py`. Файл проверяется как PDF, сохраняется в `data/uploads`, затем передается в `app/importer.py:process_pdf_api()`.

Gmail-импорт идет через `/gmail/fetch`. Настройки читаются из `.env`: IMAP-сервер, email, App Password, папка сохранения, глубина поиска, лимит писем, raw query и список отправителей. `app/gmail_fetcher.py:fetch_pdf_attachments()` подключается к Gmail, ищет письма, сохраняет PDF-вложения и возвращает списки новых, существующих и пропущенных файлов. После этого `/gmail/fetch` импортирует найденные PDF через тот же `process_pdf_api()`.

Дубликаты определяются по `receipt_number`, который берется из имени PDF без расширения. Перед вставкой `add_receipt_with_items()` проверяет наличие такого `receipt_number` в `receipts`.

### Извлечение текста и OCR

`app/importer.py:extract_text_from_pdf()` сначала пытается прочитать текстовый слой через `pdfplumber`. Если текст есть, он сразу идет в парсер.

Если текста нет, но на страницах есть изображения, проект считает PDF сканом и пробует OCR:

- конвертация PDF в PNG через `pdftoppm`;
- распознавание через `tesseract`;
- языки OCR: `lav+eng`.

Команды можно задать через переменные `PDFTOPPM_CMD` и `TESSERACT_CMD`. Если нужных программ нет, пользователь получает ошибку, что для скана нужен OCR.

### Парсер

Вход парсера - обычный текст. `parse_receipt(text)` удаляет пустые строки и решает, какой parser применить.

Если в тексте есть строка с `rimi`, вызывается `parse_rimi_receipt(lines)`. Rimi-парсер ищет дату рядом со словом `laiks`, итог по строкам `kopa`/`kopā`, товары по шаблону количества, единицы и цены. Название товара собирается из предыдущих строк.

Если Rimi-парсер не дал товаров, используется Maxima fallback. Maxima-парсер ищет дату по `YYYY-MM-DD` или `DD.MM.YYYY`, итог по строке `kopā apmaksai`, а товары по строкам вида `цена X количество`. Название также собирается из предыдущих строк.

Отдельного универсального определения магазина нет. Все, что не распознано как Rimi с товарами, фактически идет через Maxima-логику. Lidl есть в UI-фильтре аналитики, но Lidl-парсера в коде нет.

### Категоризация

Главная категоризация находится в `app/category_keywords.py:categorize_from_name()`.

Правила - это словарь `CATEGORY_KEYWORDS`: категория и список подстрок. Функция чистит название товара, оставляет слова длиной от 3 символов и считает совпадения keyword-ов. Если несколько категорий имеют одинаковый счет, выбирается категория по списку `PRIORITY`. Если совпадений нет, возвращается `прочее`.

Category Engine v2 добавляет единый список `CANONICAL_CATEGORIES`, alias mapping старых названий и `categorize_with_source()`. У каждой новой позиции есть `category_source`: `rule`, `fallback`, `manual` или `inherited`.

В `app/receipt_parser.py` есть еще `categorize_item()`, но при импорте результат перезаписывается: `app/importer.py:prepare_receipt_data()` всегда вызывает `categorize_from_name()` для каждого товара. Поэтому фактический CategoryEngine для новых импортов - `category_keywords.py`.

Категорию можно поменять вручную на страницах чека и товара. Ручная категория применяется либо к одной позиции, либо ко всей точной товарной группе. Для всей группы создается запись в `product_category_rules`; будущие импорты того же `product_key` получают эту категорию как `inherited`.

Есть отдельный скрипт `app/categorize_existing_items.py`, который может перекатегоризировать существующие товары. Он не запускается автоматически из web UI.

Для контроля категорий есть `/products/review`: экран проблемных групп, конфликтов, `прочее`, fallback и товаров без manual rule. Read-only аудит запускается через `python -m app.audit_categories`.

### SQLite

В базе главные таблицы:

- `receipts`;
- `items`.

`receipts` хранит чек: `id`, `date`, `store`, `total`, `receipt_number`.

`items` хранит позиции: `id`, `receipt_id`, `name`, `canonical_name`, `normalized_name`, `quantity`, `price`, `category`, `category_source`.

Связь:

```text
receipts.id -> items.receipt_id
```

Практически вся аналитика использует `items.price`, а не `receipts.total`. Это важно: сумма чека на главной берется из `receipts.total`, но графики расходов по категориям, месяцам и товарам считаются по товарным строкам.

В текущей базе есть 360 чеков и 3580 товарных строк; `normalized_name` заполнен у всех 3580 items. Фактическая схема старой базы немного отличается от схемы, которую код создаст для новой базы: в существующем файле `receipt_number` не имеет `UNIQUE` constraint, а внешний ключ `items.receipt_id` без `ON DELETE CASCADE`. Код все равно проверяет дубликаты по `receipt_number` перед вставкой.

`canonical_name` - важное поле для объединения похожих товаров. В аналитике используется выражение:

```sql
COALESCE(NULLIF(items.canonical_name, ''), items.name)
```

Если `canonical_name` заполнен, товар группируется по нему. Если нет - по исходному `name`.

`product_category_rules` хранит ручные категории для товарных групп. Таблица не объединяет товары и не меняет `canonical_name`.

Price & Quantity Model v2 добавляет к `items` отдельные поля для интерпретации цены: `line_total`, `unit_price`, `quantity_unit`, `package_size`, `package_unit`, `normalized_unit_price`, `normalized_price_unit`, `price_parse_source`, `price_parse_confidence`. Это слой поверх старой модели: `items.price` остается итоговой суммой строки и продолжает использоваться основными графиками расходов.

Модель evidence-first: parser-derived данные имеют confidence `0.95`, однозначная упаковка из названия (`package_name`) — `0.85`, математически согласованный `weighted_inference` — `0.75`, а неподтвержденный `inferred_piece` — `0.70`. Она сравнивает цены в EUR/L, EUR/kg или EUR/piece. Service lines, multipack, неоднозначные упаковки, parser contamination и malformed data не получают автоматическую нормализованную цену.

Backfill существующих строк вынесен в `app/backfill_price_data.py`. Dry-run является режимом по умолчанию. Write требует `--apply --sources ...`; allowlist ограничен `package_name` и `weighted_inference`. Перед записью создается SQLite Backup API backup с `PRAGMA integrity_check`, а field-level downgrade policy сохраняет более качественные значения. Выполненный selective backfill обновил 2184 строки: 2179 `package_name` и 5 `weighted_inference`; `inferred_piece`, `service_line`, rejected и unresolved не применялись. IDs 3544, 3578, 3579 и 3581 сохранены для manual review. Повторный selective dry-run показывает 0 изменений. Нормализованная цена остается unresolved у 1094 строк; полный apply не выполнялся.

## Web UI и страницы

### `/`

Главная страница показывает список чеков. Данные приходят из `get_all_receipts()`:

```sql
SELECT id, receipt_number, date, store, total
FROM receipts
ORDER BY date DESC, id DESC
```

На странице есть счетчики: количество чеков, сумма и последний чек. Поиск по магазину и сортировка таблицы выполняются в JavaScript на клиенте.

### `/upload`

Страница загрузки PDF оформлена как сфокусированный рабочий процесс, а не dashboard: основной путь последовательно показывает выбор файлов, очередь и единственное действие импорта. Поддерживаются drag-and-drop и выбор нескольких PDF; для каждого выбранного файла показываются имя и размер. Gmail вынесен в отдельный вторичный источник, чтобы не конкурировать с основным действием.

Ручная загрузка по-прежнему отправляет multipart-поле `pdfs` на `/upload`, Gmail-кнопка отправляет POST на `/gmail/fetch`. Клиентский модуль `app/web/static/upload-workspace.js` показывает состояния ожидания, успеха, предупреждения и ошибки, а ответы формирует безопасными DOM-операциями как списки импортированных, пропущенных и ошибочных файлов. Backend, importer и parser для этой миграции не менялись.

### `/analytics`

Страница аналитики сама почти не считает данные. Она загружает JSON из `/analytics/data`.

`get_analytics_data()` считает:

- расходы по категориям;
- расходы по месяцам;
- топ-10 товаров;
- общий расход;
- средний расход в месяц.

Фильтры:

- начало периода;
- конец периода;
- магазин;
- категория;
- товар.

Графики строятся в браузере через Chart.js:

- doughnut по категориям;
- line по месяцам;
- bar по топ товарам;
- line для динамики цены выбранного товара.

### `/item/<name>`

Профиль товара оформлен как документальное досье, а не dashboard. Он использует `name` и `canonical_name`, чтобы собрать историю покупок, и показывает:

- компактную идентичность товара, категорию и число появлений;
- одну связанную summary surface с последней и средней ценой, последней покупкой, числом чеков и строк;
- Price Story с текущей сопоставимой ценой, медианой, отклонением, диапазоном и честным состоянием недостаточной истории;
- существующую месячную динамику `price / quantity`, явно отделённую от нормализованной цены упаковки;
- document register всех покупок с магазином, датой, количеством, ценой, нормализованной ценой и ссылкой на чек;
- неизменённое редактирование категории для товара или отдельной позиции;
- связанные магазины, исходные названия, категорию и доступный формат упаковки.

Оценка текущей цены считается в Python функцией `build_price_evaluation()`. Если исторических точек меньше 3, оценка не показывается. Иначе последняя цена сравнивается с медианой: ниже 90% медианы - выгодно, выше 115% - дороже обычного.

Chart.js на этой странице управляется `app/web/static/item-profile.js`: canvas показывается только при наличии минимум двух валидных периодов, тема обновляет существующий график, а отсутствие или ошибка данных дают текстовое состояние вместо пустой области.

### `/receipt/<id>`

Страница одного чека. Показывает позиции, категории, количество, цену и статус цены. Данные берутся из `items` по `receipt_id`.

`build_receipt_price_analysis()` для каждого товара собирает историю цен за единицу и сравнивает текущую цену с медианой. Итоговый блок показывает:

- всего товаров;
- дешевле обычного;
- дороже обычного;
- без истории.

После Price Model v2 сравнение сначала использует `normalized_unit_price` внутри той же `normalized_price_unit`. Если таких данных нет, остается старый расчет `price / quantity`.

### `/products/merge`

Страница объединения товаров. Она не была в исходном списке страниц для документации, но влияет на аналитику. Пользователь выбирает несколько названий и задает `canonical_name`. После этого аналитика группирует эти товары как один.

### `/products/review`

Страница проверки категорий. Показывает агрегированные группы, где есть `прочее`, fallback, конфликт категорий, пустой `normalized_name`, отсутствие manual rule или пустая категория. С этой страницы можно применить canonical category ко всей точной группе.

### `/data-quality/prices`

Read-only Review / Diagnostic Workspace качества Price Model. Компактная coverage band показывает заполненность `line_total`, `unit_price` и нормализованных цен; существующий problem selector переключает unknown units, низкую confidence, отсутствующий размер упаковки и подозрительные значения. Результаты представлены плотным диагностическим register со связью с исходным чеком. Страница не исправляет данные и не запускает backfill.

## Основные ограничения

Проект сильно зависит от формата чеков. Парсер не универсальный: он подстроен под Rimi и Maxima. Если магазин меняет layout, товары могут перестать находиться.

OCR есть, но зависит от внешних программ Poppler и Tesseract. Без них PDF-сканы не читаются. Даже с OCR результат зависит от качества скана и установленных языков.

Категоризация словарная. Она не знает товаров как сущностей и не обучается от ручных исправлений. Если название не содержит известных подстрок, товар попадает в `прочее`. Если есть несколько совпадений, результат зависит от `PRIORITY`.

Старые ручные изменения категорий нельзя восстановить достоверно: раньше не было `category_source` и audit log. После миграции существующие строки получают default `rule`.

В проекте нет таблицы ошибок категоризации. Поэтому нельзя точно сказать, какие товары чаще всего определяются неправильно. Можно только смотреть частые позиции в категории `прочее`.

В проекте 171 автоматический тест. 168 проходят; три calendar-sensitive Archive/Home UI-теста всё ещё привязаны к июльским fixtures и ожидают `current_month`, хотя реальный месяц уже август 2026. Отдельной системы миграций нет. Из-за этого любые изменения схемы или парсера нужно проверять вручную на реальных PDF и read-only аудите текущей базы.

`receipt_number` берется из имени файла, не из тела чека. Если один и тот же чек пришел с другим именем файла, код может считать его новым. Если разные чеки имеют одинаковое имя файла, второй будет пропущен как дубликат.

## Где искать нужную логику

Импорт PDF:

- `app/importer.py`;
- route `/upload` в `app/web/routes.py`;
- шаблон `app/web/templates/upload.html`;
- клиентский workflow `app/web/static/upload-workspace.js`.

Gmail:

- `app/gmail_fetcher.py`;
- route `/gmail/fetch` в `app/web/routes.py`;
- настройки `.env`.

Парсер:

- `app/receipt_parser.py`.

Категории:

- `app/category_keywords.py`;
- `app/category_rules.py`;
- ручное обновление в `app/web/routes.py:update_item_category()`;
- review screen `app/web/routes.py:product_review()`;
- скрипт `app/categorize_existing_items.py`.

База:

- `app/db.py`;
- фактический файл `data/receipts.db`.

Аналитика:

- `app/analytics_service.py`;
- `app/web/routes.py:item_profile()`;
- `app/web/routes.py:view_receipt()`;
- `app/web/templates/analytics.html`;
- `app/web/templates/item.html`;
- `app/web/templates/receipt.html`.

Price Model:

- `app/price_model.py`;
- `app/backfill_price_data.py`;
- route `/data-quality/prices` в `app/web/routes.py`;
- `app/web/templates/price_data_quality.html`.

Объединение товаров:

- `app/web/routes.py:products_merge()`;
- `app/web/routes.py:products_merge_submit()`;
- `app/web/templates/products_merge.html`;
- поле `items.canonical_name`.

Новый разработчик должен помнить главный принцип этого проекта: чек сохраняется как набор товарных строк, и почти все ценное поведение строится поверх `items`. Если нужно понять расходы, категории, цену, тренды или ошибки парсинга, начинать надо с того, как конкретная строка PDF превратилась в строку `items`.
